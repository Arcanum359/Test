Задание 1
Можно полностью уйти от арифметики указателей, если переделать структуру данных

struct Packet {
	uint8_t preamble;	// преамбула = 0xFF
	uint8_t cnt;		// счетчик пакетов
	uint8_t type;		// тип пакета
	uint8_t length;		// длина поля данных
	uint8_t data[255 + 1];	// массив данных, плюс место под CRC8
}; 

на

typedef union
{
	struct
	{
		uint8_t preamble;	// преамбула = 0xFF
		uint8_t cnt;		// счетчик пакетов
		uint8_t type;		// тип пакета
		uint8_t length;		// длина поля данных
		uint8_t data[255 + 1];	// массив данных, плюс место под CRC8
	} Packet;
	uint8_t	Mas_sum[260];
}  _Packet;

В таком случае очень удобно обращаться к полям
И CRC лучше сделать отдельным полем



Задание 2
Все внесенов в один таймер
Для частоты 300 Гц сделана поправка на каждые 99 тиков